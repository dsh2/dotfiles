# vim: se ft=sshconfig:

Host HOSTNAME
# HostName NAME
# HostKeyAlias HOSTNAME
# User USERNAME
# Password PASSWORD

# VisualHostKey no
# HashKnownHosts yes
# IdentityAgent none 
IdentitiesOnly yes 
# TODO: Try to understand why IdentityFile does not support %n
IdentityFile ~/.ssh/hosts/HOSTNAME/id
# AddKeysToAgent ask
# AddKeysToAgent confirm
# AddKeysToAgent yes

# ControlPersist yes

ForwardAgent no
ForwardX11 no

# PermitLocalCommand yes
# LocalCommand  ~/.ssh/hosts/%n/local_command.sh %C %d %h %i %L %l %n %p %r %T %u &

# ClearAllForwardings yes
# ExitOnForwardFailure yes

# tinyproxy 
# RemoteForward localhost:8888 localhost:8888

# usbip
# LocalForward localhost:3240 localhost:3240 

# ssh me
# RemoteForward 2299 localhost:22
 
# DynamicForward 1080
# RemoteForward 1080

# # dev=tun99 ; sudo ip tuntap add dev $dev mode tun user $USER && sudo ip address add 10.0.2.1/24 dev $dev && sudo ip link set dev $dev up
# Tunnel point-to-point
# TunnelDevice 99:99
 
# Match host = HOSTNAME exec "nmcli con show --active | grep -q NETWORK"
# Match host = HOSTNAME exec "timeout 0.5 avahi-resolve --name HOSTNAME.local 2> /dev/null"
# HostName HOSTNAME.local
# Port 22

# Match host = HOSTNAME all
# HostName HOSTNAME
# Port 9022 
